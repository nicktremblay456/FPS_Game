Some textures are repeated in different folders for organization. As a result if you move the textures to a single folder you may get "overwrite?" warnings. Overwriting is fine since the textures that share names are exactly the same.

Textures ripped by Logan "Hyperchaotix" McCloud.